
import React from 'react';

// This component has been disabled as part of a feature revert.
// It is kept as an empty shell to prevent build errors if it is still referenced somewhere.
export const ReconstructionManager: React.FC<any> = () => {
  return null;
};
